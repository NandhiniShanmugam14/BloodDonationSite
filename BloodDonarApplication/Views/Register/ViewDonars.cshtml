@model IEnumerable<BloodDonarApplication.BloodDonationModal.Registertbl>

@{
    ViewData["Title"] = "ViewDonars";
}

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<br /><br />
<div>
    <h3 class="text-center" style="font-family: Calibri">
        All Donors
    </h3>
</div><br /><br />
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mobile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bloodgroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mobile)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bloodgroup)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            @*<td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>*@
            <td>
                <a asp-action="EditDonars" asp-route-id="@item.Id"><i class="fa fa-edit"></i></a> |
                <a asp-action="DeleteDonors" asp-route-id="@item.Id"><i class="fa fa-trash-o" style="font-size: 20px;" onclick="return confirm('Are you sure you want to delete this record?')"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
